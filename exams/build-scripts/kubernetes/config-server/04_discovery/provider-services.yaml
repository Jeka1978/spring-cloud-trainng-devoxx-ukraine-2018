apiVersion: v1
kind: Service
metadata:
  name: provider-math
  namespace: challenges-demo
spec:
  selector:
    name: backend-math
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: provider-theology
  namespace: challenges-demo
spec:
  selector:
    name: backend-theology
  ports:
    - port: 8080
      targetPort: 8080